PORT=5000
JWT_SECRET=change_this_secret_key
# Mettre true pour lancer le serveur et tester le frontend sans PostgreSQL (les routes /api échoueront sans DB).
# SKIP_DB_CHECK=true
# Option 1 (recommended): use a full PostgreSQL URL
# DATABASE_URL=postgresql://user:password@localhost:5432/share
# DB_SSL=false

# Option 2: use separate DB fields
DB_HOST=localhost
DB_PORT=5432
DB_USER=anas
DB_PASSWORD=anas123
DB_NAME=share
DB_SSL=false

# Public URL used in verification links (default: http://localhost:5000)
# APP_BASE_URL=http://localhost:5000

# Student signup email verification (SMTP)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_account@gmail.com
# SMTP_PASS=your_app_password
# SMTP_FROM="Plateforme Ressources <your_account@gmail.com>"
# STUDENT_SIGNUP_TOKEN_TTL_MINUTES=30
